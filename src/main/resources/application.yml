server:
  port: 9711

spring:
  profiles:
    active: local
  application:
    name: doms

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    metrics:
      enabled: true
  metrics:
    tags:
      application: doms